import { theme, ThemeConfig } from 'antd'

export const appTheme: ThemeConfig = {
  algorithm: theme.defaultAlgorithm,

  components: {
    Layout: {
      headerBg: '#FFFBF5',
      bodyBg: '#FFFBF5',
      footerBg: '#FFFBF5'
    }
  }
}
